builder:
  build: .
  volumes:
    - .:/src

server:
  image: nginx
  volumes:
    - ./app/public:/usr/share/nginx/html
    - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    - /etc/letsencrypt/live/te.chie.la:/etc/ssl/te.chie.la
  ports:
    - "80:80"
    - "443:443"
